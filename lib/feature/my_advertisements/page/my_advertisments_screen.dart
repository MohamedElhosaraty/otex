import 'package:flutter/material.dart';

class MyAdvertismentsScreen extends StatelessWidget {
  const MyAdvertismentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('MyAdvertismentsScreen'),
      ),
    );
  }
}
